size( 400, 100 );

int iX = 0;
int iMinY = 0;    // Y座標の最小値
int iMaxY = 50;   // Y座標の最大値
int iSize = 50;

square( iX, random( iMinY, iMaxY ), iSize );
iX += iSize;

square( iX, random( iMinY, iMaxY ), iSize );
iX += iSize;

square( iX, random( iMinY, iMaxY ), iSize );
iX += iSize;

square( iX, random( iMinY, iMaxY ), iSize );
iX += iSize;

square( iX, random( iMinY, iMaxY ), iSize );
iX += iSize;

square( iX, random( iMinY, iMaxY ), iSize );
iX += iSize;

square( iX, random( iMinY, iMaxY ), iSize );
iX += iSize;

square( iX, random( iMinY, iMaxY ), iSize );
