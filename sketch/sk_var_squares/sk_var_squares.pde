size( 400, 100 );

int iX = 0;
int iY = 25;
int iSize = 50;
int iInterval = iSize + 5;  // インターバルは正方形の大きさ + 5ピクセル

square( iX, iY, iSize );
iX += iInterval;            // インターバル分右に移動

square( iX, iY, iSize );
iX += iInterval;

square( iX, iY, iSize );
iX += iInterval;

square( iX, iY, iSize );
iX += iInterval;

square( iX, iY, iSize );
iX += iInterval;

square( iX, iY, iSize );
iX += iInterval;

square( iX, iY, iSize );
iX += iInterval;

square( iX, iY, iSize );
