size( 600, 200 );

int iX = 0;          // 円のX座標値
int iXInterval = 45; // 円間のX間隔
int iSize = 40;      // 円の大きさ

translate( 0, 100 );

circle( iX, 0, iSize );
iX += iXInterval;

circle( iX, 0, iSize );
iX += iXInterval;

circle( iX, 0, iSize );
iX += iXInterval;

circle( iX, 0, iSize );
iX += iXInterval;

circle( iX, 0, iSize );
iX += iXInterval;

circle( iX, 0, iSize );
iX += iXInterval;

circle( iX, 0, iSize );
iX += iXInterval;

circle( iX, 0, iSize );
iX += iXInterval;

circle( iX, 0, iSize );
iX += iXInterval;

circle( iX, 0, iSize );
iX += iXInterval;

circle( iX, 0, iSize );
iX += iXInterval;

circle( iX, 0, iSize );
iX += iXInterval;

circle( iX, 0, iSize );
iX += iXInterval;

circle( iX, 0, iSize );
iX += iXInterval;

save("sk_sin_cos_circles_vib_start.png");
